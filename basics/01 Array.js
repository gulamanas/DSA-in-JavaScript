const arr = [1, 3, 5, 'JavaScript'];

arr.push(7);
arr.unshift('TypeScript');
arr.pop();
arr.shift();

for (const item of arr) {
  console.log(item);
}

// map , filter, reduce, concat, slice, and splice try this out
